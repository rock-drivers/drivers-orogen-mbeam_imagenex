# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(mbeam_imagenexTaskLib)
ADD_LIBRARY(${MBEAM_IMAGENEX_TASKLIB_NAME} SHARED 
    ${MBEAM_IMAGENEX_TASKLIB_SOURCES})

add_dependencies(${MBEAM_IMAGENEX_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${MBEAM_IMAGENEX_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${MBEAM_IMAGENEX_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${MBEAM_IMAGENEX_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${MBEAM_IMAGENEX_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${MBEAM_IMAGENEX_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${MBEAM_IMAGENEX_TASKLIB_HEADERS}
    DESTINATION include/orocos/mbeam_imagenex)

